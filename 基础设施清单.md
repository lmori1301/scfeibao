# 基础设施文件清单

## 📁 目录结构

```
src/
├── api/                    # API接口层
│   ├── index.ts           # 统一导出
│   ├── party.ts           # 党建相关接口
│   ├── news.ts            # 新闻相关接口
│   ├── team.ts            # 队伍建设接口
│   ├── info.ts            # 信息公开接口
│   └── policy.ts          # 政策法规接口
│
├── types/                  # TypeScript类型定义
│   ├── common.ts          # 通用类型
│   ├── party.ts           # 党建类型
│   ├── news.ts            # 新闻类型
│   ├── team.ts            # 队伍类型
│   ├── info.ts            # 信息公开类型
│   └── policy.ts          # 政策法规类型
│
├── utils/                  # 工具函数
│   ├── http.ts            # HTTP请求封装
│   └── format.ts          # 格式化工具
│
└── mock/                   # Mock数据
    └── party.ts           # 党建Mock数据

环境配置文件:
├── .env.development       # 开发环境配置
└── .env.production        # 生产环境配置
```

---

## 📝 文件说明

### 1. HTTP请求封装 (src/utils/http.ts)

**功能**:
- ✅ 基于Axios的HTTP客户端
- ✅ 请求/响应拦截器
- ✅ 统一错误处理
- ✅ 自动显示错误提示
- ✅ 支持Token认证（预留）

**使用示例**:
```typescript
import http from '@/utils/http'

// GET请求
http.get('/api/users', { params: { page: 1 } })

// POST请求
http.post('/api/users', { name: '张三' })

// PUT请求
http.put('/api/users/1', { name: '李四' })

// DELETE请求
http.delete('/api/users/1')
```

---

### 2. 类型定义 (src/types/)

#### common.ts - 通用类型
- `ApiResponse<T>` - API响应结构
- `PageParams` - 分页参数
- `PageResponse<T>` - 分页响应
- `Option` - 选项类型

#### party.ts - 党建类型
- `PartyWorkItem` - 党建工作
- `TeamWorkItem` - 团建工作
- `MemberItem` - 党员先锋
- `StudyItem` - 党员学习

#### news.ts - 新闻类型
- `NewsItem` - 新闻条目
- `NewsListParams` - 新闻列表参数

#### team.ts - 队伍建设类型
- `CaseItem` - 救援案例
- `ShowcaseItem` - 队伍风采
- `TeamInfo` - 队伍信息

#### info.ts - 信息公开类型
- `PersonnelItem` - 人事任免

#### policy.ts - 政策法规类型
- `LawItem` - 法律法规
- `RegulationItem` - 部门规章
- `StandardItem` - 行业标准

---

### 3. API接口层 (src/api/)

#### party.ts - 党建接口
```typescript
// 获取党建工作列表
getPartyWorkList(params)

// 获取党建工作详情
getPartyWorkDetail(id)

// 获取团建工作列表
getTeamWorkList(params)

// 获取团建工作详情
getTeamWorkDetail(id)

// 获取党员先锋列表
getMembersList(params)

// 获取党员先锋详情
getMemberDetail(id)

// 获取党员学习列表
getStudyList(params)

// 获取党员学习详情
getStudyDetail(id)
```

#### news.ts - 新闻接口
```typescript
// 获取新闻列表
getNewsList(params)

// 获取新闻详情
getNewsDetail(id)

// 获取最新新闻
getLatestNews(limit)
```

#### team.ts - 队伍建设接口
```typescript
// 获取救援案例列表
getCasesList(params)

// 获取救援案例详情
getCaseDetail(id)

// 获取队伍风采列表
getShowcaseList(params)

// 获取队伍风采详情
getShowcaseDetail(id)

// 获取队伍信息
getTeamInfo()
```

#### info.ts - 信息公开接口
```typescript
// 获取人事任免列表
getPersonnelList(params)

// 获取人事任免详情
getPersonnelDetail(id)
```

#### policy.ts - 政策法规接口
```typescript
// 获取法律法规列表
getLawsList(params)

// 获取法律法规详情
getLawDetail(id)

// 获取部门规章列表
getRegulationsList(params)

// 获取部门规章详情
getRegulationDetail(id)

// 获取行业标准列表
getStandardsList(params)

// 获取行业标准详情
getStandardDetail(id)
```

---

### 4. 格式化工具 (src/utils/format.ts)

**工具函数**:
```typescript
// 格式化日期
formatDate(date, format) // '2025-12-06'

// 格式化相对时间
formatRelativeTime(date) // '3天前'

// 截断文本
truncateText(text, maxLength, suffix) // '这是一段长文本...'

// 格式化数字
formatNumber(1234567) // '1,234,567'

// 格式化文件大小
formatFileSize(1024) // '1.00 KB'
```

---

### 5. Mock数据 (src/mock/)

#### party.ts - 党建Mock数据
```typescript
// Mock党建工作列表
mockPartyWorkList

// 获取Mock列表
getMockPartyWorkList(page, pageSize)

// 获取Mock详情
getMockPartyWorkDetail(id)
```

---

## 🔧 环境配置

### .env.development (开发环境)
```bash
VITE_APP_BASE_API=/api
VITE_APP_TITLE=四川飞豹救援（开发环境）
```

### .env.production (生产环境)
```bash
VITE_APP_BASE_API=https://api.feibao-rescue.com/api
VITE_APP_TITLE=四川飞豹救援
```

---

## 📦 依赖包

项目需要安装以下依赖：

```bash
# Axios
npm install axios

# Element Plus (如未安装)
npm install element-plus

# TypeScript类型
npm install -D @types/node
```

---

## 🚀 使用示例

### 在组件中使用API

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { getPartyWorkList } from '@/api/party'
import { formatDate } from '@/utils/format'
import type { PartyWorkItem } from '@/types/party'

// 响应式数据
const list = ref<PartyWorkItem[]>([])
const loading = ref(false)

// 获取数据
const fetchList = async () => {
  loading.value = true
  try {
    const res = await getPartyWorkList({
      page: 1,
      pageSize: 10
    })
    list.value = res.data.list
  } catch (error) {
    console.error('获取数据失败:', error)
  } finally {
    loading.value = false
  }
}

// 初始化
onMounted(() => {
  fetchList()
})
</script>

<template>
  <div v-loading="loading">
    <div v-for="item in list" :key="item.id">
      <h3>{{ item.title }}</h3>
      <p>{{ item.summary }}</p>
      <p>{{ formatDate(item.publishDate) }}</p>
    </div>
  </div>
</template>
```

### 使用Mock数据

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { getMockPartyWorkList } from '@/mock/party'

const list = ref([])

// 使用Mock数据
const res = getMockPartyWorkList(1, 10)
list.value = res.data.list
</script>
```

---

## 📋 下一步

1. ✅ 基础设施已创建完成
2. ⏳ 可以开始优化组件代码
3. ⏳ 对接真实后端API
4. ⏳ 完善Mock数据

---

**创建日期**: 2025-01-28
**维护者**: 开发团队
