# MySQL 初始化目录

`10-schema.sql` 提供当前 27 张业务表的完整结构，不包含任何历史业务数据或账号凭证。

如需初始化经过审核的基础数据，可另行添加 `20-seed.sql`。生产业务数据应通过备份恢复流程导入，不应提交到仓库。

MySQL 官方镜像仅在数据卷为空时按文件名顺序执行本目录中的 `.sql`、`.sql.gz` 和 `.sh` 文件。不要把生产密码或未经脱敏的数据提交到仓库。
