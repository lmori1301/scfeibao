# 后端功能需求文档
---

## 一、文档概述
### 1.1 文档目的
本文档明确四川飞豹救援系统后端API的核心功能逻辑、接口规范、数据模型、业务规则及安全策略，为后端开发、接口对接、测试验证提供统一依据，确保API设计合理、数据安全可靠、业务逻辑清晰。

### 1.2 适用范围
适用于"四川飞豹救援"官网前端系统与后台管理系统的后端API开发与迭代，覆盖认证授权、内容管理、用户管理、查询系统、文件上传、系统设置六大核心模块。

### 1.3 核心目标
- 构建RESTful API规范，确保接口设计统一、易于维护；
- 实现JWT认证机制，保障API访问安全；
- 建立分级权限体系，控制数据访问范围；
- 优化数据库设计，提升查询性能；
- 提供完善的错误处理，便于问题定位。

---

## 二、技术架构
### 2.1 技术栈
- **框架**: NestJS (Node.js)
- **ORM**: TypeORM
- **数据库**: MySQL 8.0
- **认证**: JWT (JSON Web Token)
- **文件存储**: 本地存储 / 云存储
- **API文档**: Swagger

### 2.2 架构模式
- **分层架构**: Controller → Service → Repository
- **模块化设计**: 按业务功能划分模块
- **依赖注入**: 使用NestJS依赖注入容器
- **全局守卫**: JWT认证守卫、权限守卫
- **全局拦截器**: 响应格式统一拦截器
- **全局过滤器**: 异常处理过滤器

---

## 三、核心模块功能需求
### 3.1 认证授权模块 (AuthModule)
#### 3.1.1 模块定位
提供用户登录、注册、JWT token生成与验证功能，是系统安全的基础模块。

#### 3.1.2 登录接口
- **接口路径**: `POST /api/auth/login`
- **请求参数**: `{ username, password }`
- **业务逻辑**:
  1. 验证用户名密码是否正确（使用bcrypt比对）
  2. 检查用户状态是否启用
  3. 生成JWT token（有效期7天）
  4. 更新最后登录时间
  5. 返回token和用户基本信息
- **响应数据**: `{ token, user: { id, username, realName, role } }`
- **错误处理**: 401-用户名或密码错误，401-账号已被禁用

#### 3.1.3 注册接口
- **接口路径**: `POST /api/auth/register`
- **请求参数**: `{ username, password, realName }`
- **业务逻辑**:
  1. 检查用户名是否已存在
  2. 密码加密（bcrypt，salt rounds=10）
  3. 创建用户记录（默认角色viewer，状态启用）
  4. 返回用户信息（不含密码）
- **响应数据**: `{ id, username, realName, role, status }`
- **错误处理**: 409-用户名已存在

#### 3.1.4 JWT验证策略
- **验证逻辑**:
  1. 从请求头提取Bearer token
  2. 验证token签名和有效期
  3. 解析payload获取用户信息
  4. 将用户信息注入请求对象
- **公开接口**: 使用@Public()装饰器标记无需认证的接口

### 3.2 用户管理模块 (AdminUserModule)
#### 3.2.1 管理员列表接口
- **接口路径**: `GET /api/admin/users`
- **请求参数**: `{ page, pageSize, keyword, role, status }`
- **业务逻辑**:
  1. 根据筛选条件查询管理员列表
  2. 支持按用户名、姓名模糊搜索
  3. 支持按角色、状态筛选
  4. 分页返回结果
- **响应数据**: `{ items: [], total, page, pageSize }`
- **权限要求**: 需要admin或super_admin角色

#### 3.2.2 创建管理员接口
- **接口路径**: `POST /api/admin/users`
- **请求参数**: `{ username, password, name, email, phone, role }`
- **业务逻辑**:
  1. 验证用户名唯一性
  2. 密码加密存储
  3. 创建管理员记录
  4. 记录操作日志
- **权限要求**: 需要super_admin角色

#### 3.2.3 更新管理员接口
- **接口路径**: `PUT /api/admin/users/:id`
- **请求参数**: `{ name, email, phone, role, status }`
- **业务逻辑**:
  1. 验证管理员是否存在
  2. 更新管理员信息
  3. 记录操作日志
- **权限要求**: 需要super_admin角色

#### 3.2.4 删除管理员接口
- **接口路径**: `DELETE /api/admin/users/:id`
- **业务逻辑**:
  1. 软删除管理员记录
  2. 记录操作日志
- **权限要求**: 需要super_admin角色

### 3.3 角色权限模块 (RoleModule)
#### 3.3.1 角色列表接口
- **接口路径**: `GET /api/admin/roles`
- **响应数据**: 所有角色列表及权限配置
- **权限要求**: 需要admin或super_admin角色

#### 3.3.2 创建角色接口
- **接口路径**: `POST /api/admin/roles`
- **请求参数**: `{ name, description, permissions }`
- **业务逻辑**:
  1. 验证角色名称唯一性
  2. 创建角色记录
  3. 配置角色权限
- **权限要求**: 需要super_admin角色

#### 3.3.3 更新角色接口
- **接口路径**: `PUT /api/admin/roles/:id`
- **请求参数**: `{ name, description, permissions, status }`
- **业务逻辑**:
  1. 更新角色信息
  2. 更新权限配置
- **权限要求**: 需要super_admin角色

### 3.4 新闻管理模块 (NewsModule)
#### 3.4.1 新闻列表接口（公开）
- **接口路径**: `GET /api/news`
- **请求参数**: `{ page, pageSize, category, keyword }`
- **业务逻辑**:
  1. 查询已发布状态的新闻
  2. 支持按分类筛选
  3. 支持按标题关键词搜索
  4. 按发布时间倒序排列
  5. 分页返回结果
- **响应数据**: `{ items: [], total, page, pageSize }`
- **权限要求**: 公开接口，无需认证

#### 3.4.2 新闻详情接口（公开）
- **接口路径**: `GET /api/news/:id`
- **业务逻辑**:
  1. 查询新闻详情
  2. 增加浏览次数
  3. 返回完整内容
- **响应数据**: 新闻完整信息
- **权限要求**: 公开接口，无需认证

#### 3.4.3 创建新闻接口
- **接口路径**: `POST /api/news`
- **请求参数**: `{ title, content, summary, coverImage, category, author }`
- **业务逻辑**:
  1. 创建新闻记录（默认状态为草稿）
  2. 记录创建人和创建时间
  3. 返回新闻信息
- **权限要求**: 需要editor或admin角色

#### 3.4.4 更新新闻接口
- **接口路径**: `PATCH /api/news/:id`
- **请求参数**: `{ title, content, summary, coverImage, category, status }`
- **业务逻辑**:
  1. 验证新闻是否存在
  2. 更新新闻信息
  3. 状态流转控制（草稿→待审→发布→下架）
  4. 记录操作日志
- **权限要求**: 需要editor或admin角色

#### 3.4.5 删除新闻接口
- **接口路径**: `DELETE /api/news/:id`
- **业务逻辑**:
  1. 软删除新闻记录
  2. 记录操作日志
- **权限要求**: 需要admin角色

### 3.5 证书管理模块 (CertificateModule)
#### 3.5.1 证书查询接口（公开）
- **接口路径**: `GET /api/certificates`
- **请求参数**: `{ certificateNo, name }`
- **业务逻辑**:
  1. 根据证书编号或姓名查询
  2. 只返回有效状态的证书
  3. 计算证书有效期状态
- **响应数据**: 证书信息列表
- **权限要求**: 公开接口，无需认证

#### 3.5.2 证书列表接口（管理）
- **接口路径**: `GET /api/admin/certificates`
- **请求参数**: `{ page, pageSize, status, keyword }`
- **业务逻辑**:
  1. 查询所有证书记录
  2. 支持按状态筛选
  3. 支持按证书编号、姓名搜索
  4. 标识即将到期证书（30天内）
  5. 分页返回结果
- **权限要求**: 需要认证

#### 3.5.3 创建证书接口
- **接口路径**: `POST /api/admin/certificates`
- **请求参数**: `{ certificateNo, name, type, issueDate, expiryDate }`
- **业务逻辑**:
  1. 验证证书编号唯一性
  2. 创建证书记录
  3. 记录操作日志
- **权限要求**: 需要editor或admin角色

#### 3.5.4 批量导入证书接口
- **接口路径**: `POST /api/admin/certificates/import`
- **请求参数**: Excel文件
- **业务逻辑**:
  1. 解析Excel文件
  2. 验证数据格式
  3. 批量插入证书记录
  4. 返回导入结果统计
- **权限要求**: 需要admin角色

### 3.6 车辆管理模块 (VehicleModule)
#### 3.6.1 车辆查询接口（公开）
- **接口路径**: `GET /api/vehicles`
- **请求参数**: `{ plateNo, vehicleNo }`
- **业务逻辑**:
  1. 根据车牌号或车辆编号查询
  2. 返回车辆基本信息和状态
- **响应数据**: 车辆信息列表
- **权限要求**: 公开接口，无需认证

#### 3.6.2 车辆列表接口（管理）
- **接口路径**: `GET /api/admin/vehicles`
- **请求参数**: `{ page, pageSize, status, keyword }`
- **业务逻辑**:
  1. 查询所有车辆记录
  2. 支持按状态筛选
  3. 支持按车牌号、车辆编号搜索
  4. 分页返回结果
- **权限要求**: 需要认证

#### 3.6.3 创建车辆接口
- **接口路径**: `POST /api/admin/vehicles`
- **请求参数**: `{ plateNo, vehicleNo, type, unit, status }`
- **业务逻辑**:
  1. 验证车牌号唯一性
  2. 创建车辆记录
  3. 记录操作日志
- **权限要求**: 需要editor或admin角色

### 3.7 人员管理模块 (PersonnelModule)
#### 3.7.1 人员查询接口（需认证）
- **接口路径**: `GET /api/personnel`
- **请求参数**: `{ name, employeeNo }`
- **业务逻辑**:
  1. 验证用户登录状态
  2. 根据姓名或工号查询
  3. 返回人员基本信息
- **权限要求**: 需要认证

#### 3.7.2 人员列表接口（管理）
- **接口路径**: `GET /api/admin/personnel`
- **请求参数**: `{ page, pageSize, status, department, keyword }`
- **业务逻辑**:
  1. 查询所有人员记录
  2. 支持按状态、部门筛选
  3. 支持按姓名、工号搜索
  4. 分页返回结果
- **权限要求**: 需要认证

#### 3.7.3 创建人员接口
- **接口路径**: `POST /api/admin/personnel`
- **请求参数**: `{ name, employeeNo, department, position, status }`
- **业务逻辑**:
  1. 验证工号唯一性
  2. 创建人员记录
  3. 记录操作日志
- **权限要求**: 需要editor或admin角色

### 3.8 文件上传模块 (UploadModule)
#### 3.8.1 图片上传接口
- **接口路径**: `POST /api/admin/upload/image`
- **请求参数**: 图片文件（multipart/form-data）
- **业务逻辑**:
  1. 验证文件类型（jpg/jpeg/png/gif）
  2. 验证文件大小（≤5MB）
  3. 生成唯一文件名
  4. 保存到指定目录
  5. 返回文件访问URL
- **响应数据**: `{ url, filename, size }`
- **权限要求**: 需要认证

#### 3.8.2 视频上传接口
- **接口路径**: `POST /api/admin/upload/video`
- **请求参数**: 视频文件（multipart/form-data）
- **业务逻辑**:
  1. 验证文件类型（mp4/avi/mov）
  2. 验证文件大小（≤1GB）
  3. 生成唯一文件名
  4. 保存到指定目录
  5. 返回文件访问URL
- **响应数据**: `{ url, filename, size }`
- **权限要求**: 需要认证

#### 3.8.3 文件上传接口
- **接口路径**: `POST /api/admin/upload/file`
- **请求参数**: 文件（multipart/form-data）
- **业务逻辑**:
  1. 验证文件类型（pdf/doc/docx/xls/xlsx）
  2. 验证文件大小（≤20MB）
  3. 生成唯一文件名
  4. 保存到指定目录
  5. 返回文件访问URL
- **响应数据**: `{ url, filename, size }`
- **权限要求**: 需要认证

### 3.9 工作台模块 (DashboardModule)
#### 3.9.1 数据统计接口
- **接口路径**: `GET /api/admin/dashboard/stats`
- **业务逻辑**:
  1. 统计新闻总数和趋势
  2. 统计证书总数和状态分布
  3. 统计车辆总数和状态分布
  4. 统计人员总数和状态分布
  5. 返回统计数据
- **响应数据**: `{ news, certificates, vehicles, personnel }`
- **权限要求**: 需要认证

#### 3.9.2 待办事项接口
- **接口路径**: `GET /api/admin/dashboard/todos`
- **业务逻辑**:
  1. 查询待审核新闻（最多5条）
  2. 查询即将到期证书（30天内，最多10条）
  3. 查询待维护车辆（最多5条）
  4. 返回待办列表
- **响应数据**: `{ pendingNews, expiringCertificates, maintenanceVehicles }`
- **权限要求**: 需要认证

#### 3.9.3 操作日志接口
- **接口路径**: `GET /api/admin/dashboard/logs`
- **请求参数**: `{ page, pageSize, type }`
- **业务逻辑**:
  1. 查询最近操作日志
  2. 支持按操作类型筛选
  3. 分页返回结果
- **响应数据**: `{ items: [], total }`
- **权限要求**: 需要认证

#### 3.9.4 系统通知接口
- **接口路径**: `GET /api/admin/dashboard/notifications`
- **业务逻辑**:
  1. 查询未读通知
  2. 按时间倒序排列
  3. 返回通知列表
- **响应数据**: 通知列表
- **权限要求**: 需要认证

### 3.10 系统设置模块 (SettingModule)
#### 3.10.1 获取系统设置接口
- **接口路径**: `GET /api/admin/settings`
- **业务逻辑**:
  1. 查询所有系统设置项
  2. 返回设置键值对
- **响应数据**: 设置项列表
- **权限要求**: 需要admin角色

#### 3.10.2 更新系统设置接口
- **接口路径**: `PUT /api/admin/settings/:key`
- **请求参数**: `{ value }`
- **业务逻辑**:
  1. 验证设置项是否存在
  2. 更新设置值
  3. 记录操作日志
- **权限要求**: 需要super_admin角色

#### 3.10.3 批量更新设置接口
- **接口路径**: `PUT /api/admin/settings/batch/update`
- **请求参数**: `{ settings: [{ key, value }] }`
- **业务逻辑**:
  1. 批量更新设置项
  2. 记录操作日志
- **权限要求**: 需要super_admin角色

---

## 四、数据模型设计
### 4.1 用户表 (users)
```
id: 主键
username: 用户名（唯一）
password: 密码（bcrypt加密）
realName: 真实姓名
role: 角色（admin/editor/viewer）
status: 状态（1启用/0禁用）
lastLoginAt: 最后登录时间
createdAt: 创建时间
updatedAt: 更新时间
deletedAt: 删除时间（软删除）
```

### 4.2 管理员表 (admin_users)
```
id: 主键
username: 用户名（唯一）
password: 密码（bcrypt加密）
name: 姓名
email: 邮箱
phone: 电话
role: 角色
status: 状态（active/inactive）
lastLoginTime: 最后登录时间
createTime: 创建时间
updateTime: 更新时间
```

### 4.3 角色表 (roles)
```
id: 主键
name: 角色名称（唯一）
description: 角色描述
permissions: 权限列表（JSON）
status: 状态（1启用/0禁用）
createdAt: 创建时间
updatedAt: 更新时间
```

### 4.4 新闻表 (news)
```
id: 主键
title: 标题
content: 内容
summary: 摘要
coverImage: 封面图
category: 分类
author: 作者
status: 状态（0草稿/1待审/2已发布/3已下架）
viewCount: 浏览次数
publishedAt: 发布时间
sort: 排序
createdAt: 创建时间
updatedAt: 更新时间
deletedAt: 删除时间（软删除）
```

### 4.5 证书表 (certificates)
```
id: 主键
certificateNo: 证书编号（唯一）
name: 姓名
type: 证书类型
issueDate: 发证日期
expiryDate: 有效期至
status: 状态（1有效/0失效）
createdAt: 创建时间
updatedAt: 更新时间
```

### 4.6 车辆表 (vehicles)
```
id: 主键
plateNo: 车牌号（唯一）
vehicleNo: 车辆编号
type: 车辆类型
unit: 所属单位
status: 状态（1正常/2维修/3报废）
createdAt: 创建时间
updatedAt: 更新时间
```

### 4.7 人员表 (personnel)
```
id: 主键
name: 姓名
employeeNo: 工号（唯一）
department: 部门
position: 职务
status: 状态（1在职/2休假/3培训/4离职）
createdAt: 创建时间
updatedAt: 更新时间
```

### 4.8 系统设置表 (settings)
```
id: 主键
key: 设置键（唯一）
value: 设置值
description: 描述
createdAt: 创建时间
updatedAt: 更新时间
```

---

## 五、API响应规范
### 5.1 统一响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": 1234567890
}
```

### 5.2 状态码定义
- **200**: 请求成功
- **201**: 创建成功
- **400**: 请求参数错误
- **401**: 未授权（未登录或token失效）
- **403**: 禁止访问（无权限）
- **404**: 资源不存在
- **409**: 资源冲突（如用户名已存在）
- **500**: 服务器内部错误

### 5.3 分页响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "items": [],
    "total": 100,
    "page": 1,
    "pageSize": 10
  },
  "timestamp": 1234567890
}
```

### 5.4 错误响应格式
```json
{
  "code": 400,
  "message": "请求参数错误",
  "timestamp": 1234567890
}
```

---

## 六、安全策略
### 6.1 认证机制
- 使用JWT token进行身份认证
- Token有效期7天
- Token存储在请求头：`Authorization: Bearer <token>`
- 公开接口使用@Public()装饰器标记

### 6.2 密码安全
- 使用bcrypt加密存储密码
- Salt rounds设置为10
- 密码长度要求≥6位

### 6.3 权限控制
- 基于角色的访问控制（RBAC）
- 角色层级：super_admin > admin > editor > viewer
- 接口级权限验证

### 6.4 数据安全
- 敏感数据加密存储
- SQL注入防护（使用ORM参数化查询）
- XSS防护（输入验证和输出转义）
- CSRF防护（使用token验证）

### 6.5 日志审计
- 记录所有关键操作日志
- 日志包含：操作人、操作时间、操作类型、操作内容
- 日志永久保存，支持查询和导出

---

## 七、性能优化
### 7.1 数据库优化
- 为常用查询字段添加索引
- 使用连接池管理数据库连接
- 分页查询避免全表扫描
- 软删除避免物理删除

### 7.2 缓存策略
- 静态配置数据缓存
- 热点数据缓存
- 缓存过期策略

### 7.3 接口优化
- 响应数据压缩
- 批量操作接口
- 异步处理耗时任务

---

## 八、开发规范
### 8.1 代码规范
- 使用TypeScript严格模式
- 遵循ESLint规则
- 统一代码格式（Prettier）

### 8.2 命名规范
- 模块名：PascalCase（如NewsModule）
- 服务名：PascalCase + Service（如NewsService）
- 控制器名：PascalCase + Controller（如NewsController）
- 接口路径：kebab-case（如/api/admin/users）

### 8.3 注释规范
- 模块、类、方法添加注释说明
- 复杂业务逻辑添加行内注释
- 使用JSDoc格式注释

---

**文档版本**: v1.0
**最后更新**: 2026-02-19
