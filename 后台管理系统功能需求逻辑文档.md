# 后台管理系统功能需求逻辑文档
---

## 一、文档概述
### 1.1 文档目的
本文档明确后台管理系统的核心功能逻辑、业务流程、数据规则及交互规范，为产品设计、开发实现、测试验证、运维管理提供统一依据，确保系统功能覆盖业务场景、数据流转清晰、权限控制严谨。

### 1.2 适用范围
适用于支撑“四川飞豹救援”官网的后台管理系统全模块开发与迭代，覆盖工作台、内容管理、用户与权限管理、查询系统管理、系统设置五大核心模块。

### 1.3 核心目标
- 打造高效工作台，聚合核心信息与高频操作，提升后台运维效率；
- 统一内容管理规范，实现新闻、人事、政策等信息的高效发布与维护；
- 构建分级权限体系，保障数据安全与操作可控；
- 支撑证书、车辆、人员等核心业务数据的全生命周期管理；
- 提供系统级配置与运维能力，保障系统稳定运行。

---

## 二、菜单层级结构
| 一级菜单 | 二级菜单 | 核心责任人 |
|----------|----------|------------|
| 工作台   | 数据概览、待办事项、操作日志、系统通知、快捷入口 | 前端开发/产品 |
| 内容管理 | banner轮播图、新闻发布管理、人事任免管理、政策文件管理、领导信息、地理位置、党建专栏、队伍介绍、救援案例、队伍风采管理 | 内容开发/产品 |
| 用户与权限管理 | 管理员管理、人员管理、权限组设置 | 后端开发/测试 |
| 查询系统管理 | 证书管理、车辆管理、内部管理 | 业务开发/测试 |
| 系统设置 | 网站配置、导航栏设置、数据备份与恢复、操作日志管理、友情链接管理 | 运维开发/测试 |

---

## 三、核心模块功能需求逻辑
### 3.1 工作台模块（一级菜单）
#### 3.1.1 模块定位
工作台是管理员登录系统后的默认首页，作为核心信息聚合、待办任务提醒、高频操作直达的综合入口，实现“一站式”管理，减少模块切换成本。

#### 3.1.2 数据概览（二级菜单）
- **功能目标**：实时展示核心业务数据，快速掌握系统整体状态。
- **核心逻辑**：
  1.  数据卡片：新闻数据（今日发布/待审核/已下架+同比）、证书数据（有效/即将到期/已失效）、车辆数据（正常/维修/报废）、人员数据（在职/休假/培训）；
  2.  视觉规则：即将到期/异常状态数据标红提示；
  3.  交互规则：点击卡片跳转对应模块列表页；
  4.  数据规则：复用现有模块接口，5分钟定时刷新+手动刷新。

#### 3.1.3 待办事项（二级菜单）
- **功能目标**：集中展示关键待办，避免任务遗漏，提升流转效率。
- **核心逻辑**：
  1.  待办分类（按优先级）：待审核新闻、即将到期证书、待维护车辆；
  2.  展示规则：待审核新闻展示5条、即将到期证书展示10条、待维护车辆展示5条；
  3.  操作规则：支持“查看详情”“一键处理”（审核/续期/标记完成），操作后实时刷新列表；
  4.  筛选规则：支持按待办类型筛选。

#### 3.1.4 操作日志（二级菜单）
- **功能目标**：展示近期操作记录，便于追溯与问题排查。
- **核心逻辑**：
  1.  展示规则：默认展示最近20条记录，包含操作人员、时间、类型、内容；
  2.  筛选规则：支持按操作类型（登录/新增/编辑/删除）筛选；
  3.  交互规则：点击记录查看详情（如编辑前后对比）；
  4.  数据规则：复用系统设置模块日志接口，永久追溯。

#### 3.1.5 系统通知（二级菜单）
- **功能目标**：同步系统公告与自动提醒，确保关键信息不遗漏。
- **核心逻辑**：
  1.  通知分类：系统公告（超级管理员发布）、自动提醒（证书到期/备份结果/操作异常）；
  2.  视觉规则：未读通知标红；
  3.  交互规则：点击查看详情，支持“标记已读/全部已读”；
  4.  存储规则：通知保留30天，过期自动清理。

#### 3.1.6 快捷入口（二级菜单）
- **功能目标**：提供高频操作直达入口，减少操作路径。
- **核心逻辑**：
  1.  入口分类：高频业务（发布新闻/新增证书/查询车辆/新增人员）、运维操作（手动备份/查看日志/系统设置）；
  2.  权限规则：无对应权限则隐藏入口；
  3.  交互规则：点击跳转对应功能页，表单预加载当前用户信息。

### 3.2 内容管理模块（一级菜单）
#### 3.2.1 banner轮播图（二级菜单）
- **功能目标**：管理官网首页轮播图，支持动态更新。
- **核心逻辑**：
  1.  操作规则：支持发布/编辑/删除，上传轮播图文件；
  2.  关联规则：轮播图需绑定标题（用于检索）；
  3.  同步规则：操作后实时同步至前端。

#### 3.2.2 新闻发布管理（二级菜单）
- **功能目标**：实现新闻全生命周期管理。
- **核心逻辑**：
  1.  创作规则：支持图文/视频编辑，视频大小≤1GB；
  2.  状态流转：草稿→待审→发布→下架，按权限控制审核节点；
  3.  分类规则：关联标签（动态要闻/各地动态/救援行动/政策解决/媒体播报）；
  4.  批量操作：支持批量下架/删除，操作后同步前端。

#### 3.2.3 人事任免管理（二级菜单）
- **功能目标**：发布人事任免信息，关联附件。
- **核心逻辑**：
  1.  信息结构：标题+文号+发布日期+生效日期+发布部门+附件；
  2.  展示规则：前端按发布时间倒序展示，支持部门/文号检索。

#### 3.2.4 政策文件管理（二级菜单）
- **功能目标**：管理政策文件，支持分类归档。
- **核心逻辑**：
  1.  分类规则：绑定标签（法律法规/部门规章/行业标准）；
  2.  信息结构：同人事任免管理，支持附件上传；
  3.  展示规则：仅展示有效文件（未到失效日期）。

#### 3.2.5 领导信息（二级菜单）
- **功能目标**：维护领导团队信息，支持批量导入导出。
- **核心逻辑**：
  1.  录入规则：单条录入/Excel批量导入，字段包含姓名/职位/民族/救援经验等；
  2.  维护规则：支持编辑/状态更新/单条/批量删除；
  3.  导出规则：Excel导出字段与前端展示一致。

#### 3.2.6 地理位置（二级菜单）
- **功能目标**：管理队伍地理位置，支持地图展示。
- **核心逻辑**：
  1.  信息字段：队伍名称/地址/电话；
  2.  集成规则：地址调用第三方API实现地图定位。

#### 3.2.7 党建专栏（二级菜单）
- **功能目标**：发布党建内容，支持分类管理。
- **核心逻辑**：
  1.  内容类型：新闻/图文；
  2.  分类规则：绑定标签（党建工作/团建工作/党员先锋/党员学习）。

#### 3.2.8 队伍介绍/救援案例（二级菜单）
- **功能目标**：展示队伍形象与救援成果。
- **核心逻辑**：
  1.  操作规则：增改查标题/新闻/图文，支持图片上传；
  2.  优化规则：图片自动压缩，提升加载速度。

#### 3.2.9 队伍风采管理（二级菜单）
- **功能目标**：管理训练/救援/文化活动内容。
- **核心逻辑**：
  1.  操作规则：增改查/排序图文/视频，视频≤1GB；
  2.  优化规则：图片自动压缩。

### 3.3 用户与权限管理模块（一级菜单）
#### 3.3.1 管理员管理（二级菜单）
- **功能目标**：维护管理员账号，控制账号状态。
- **核心逻辑**：
  1.  操作规则：新增/删除账号、修改密码；
  2.  状态规则：禁用账号无法登录系统。

#### 3.3.2 人员管理（二级菜单）
- **功能目标**：维护系统用户信息，支持批量操作。
- **核心逻辑**：
  1.  信息字段：姓名/部门/职务/入职日期/状态（在职/离职）；
  2.  批量操作：Excel导入（含必填项校验）/导出（支持字段筛选）；
  3.  状态规则：离职人员账号自动禁用。

#### 3.3.3 权限组设置（二级菜单）
- **功能目标**：构建分级权限体系，支持细粒度控制。
- **核心逻辑**：
  1.  预设组：超级管理员/内容编辑/审核员/查询员；
  2.  自定义规则：支持模块+操作级权限配置；
  3.  继承规则：子账号继承组权限，可单独调整。

### 3.4 查询系统管理模块（一级菜单）
#### 3.4.1 证书管理（二级菜单）
- **功能目标**：管理救援人员证书，支持有效期预警。
- **核心逻辑**：
  1.  录入规则：单条/Excel批量导入，字段含证书编号/类型/有效期等；
  2.  类型规则：应急指挥专家/绳索救援技术员等10类；
  3.  状态规则：有效期前30天标红预警，到期自动失效；
  4.  导出规则：按类型/状态筛选导出Excel。

#### 3.4.2 车辆管理（二级菜单）
- **功能目标**：管理救援车辆，跟踪状态。
- **核心逻辑**：
  1.  录入规则：单条/Excel批量导入，字段含车辆编号/类型/状态等；
  2.  类型规则：应急救援指挥车/救援运输车等12类；
  3.  状态规则：正常/待命/应急/备勤/维修/报废/保险；
  4.  导出规则：按类型/状态筛选导出Excel。

#### 3.4.3 内部管理（二级菜单）
- **功能目标**：维护内部人员信息，支持数据同步。
- **核心逻辑**：
  1.  录入规则：单条/Excel批量导入，字段含人员编号/任务次数/培训时长等；
  2.  校验规则：导入模板含必填项，日期格式为YYYY-MM-DD；
  3.  同步规则：任务次数/培训时长支持手动/自动同步；
  4.  状态规则：在职/离职/休假/公差/培训；
  5.  导出规则：按部门/状态筛选导出Excel。

### 3.5 系统设置模块（一级菜单）
#### 3.5.1 网站配置（二级菜单）
- **功能目标**：配置官网基础信息，控制前端展示。
- **核心逻辑**：
  1.  配置项：网站名称/LOGO/版权信息/默认轮播图；
  2.  格式规则：LOGO/图片仅支持png/jpg。

#### 3.5.2 导航栏设置（二级菜单）
- **功能目标**：管理官网导航栏，控制层级与显示。
- **核心逻辑**：
  1.  操作规则：新增/隐藏/调整顺序，支持拖拽排序；
  2.  层级规则：最多支持2级子栏目；
  3.  显示规则：可配置栏目是否在前端展示。

#### 3.5.3 数据备份与恢复（二级菜单）
- **功能目标**：保障数据安全，支持备份与恢复。
- **核心逻辑**：
  1.  备份规则：自动备份（周期≤24小时）+手动备份；
  2.  管理规则：支持备份文件查看/下载/恢复；
  3.  存储规则：备份保留30天，避免冗余。

#### 3.5.4 操作日志管理（二级菜单）
- **功能目标**：记录所有操作行为，支持审计追溯。
- **核心逻辑**：
  1.  记录规则：覆盖登录/新增/编辑/删除等所有操作；
  2.  筛选规则：支持按时间/操作人员/类型筛选；
  3.  存储规则：日志永久保存，用于安全审计。

#### 3.5.5 友情链接管理（二级菜单）
- **功能目标**：管理官网友情链接，保障有效性。
- **核心逻辑**：
  1.  操作规则：新增/删除/修改名称/URL/排序；
  2.  校验规则：新增/修改时自动检测链接有效性；
  3.  显示规则：可配置是否在前端展示。

---

## 四、全局业务规则
1.  **权限控制**：所有操作校验权限，无权限提示“权限不足”；工作台内容/快捷入口按权限显示/隐藏；
2.  **数据一致性**：导入/导出字段与前端一致，核心数据状态流转后同步至工作台；
3.  **媒体处理**：图片/视频自动压缩，视频≤1GB，工作台复用压缩后资源；
4.  **API集成**：地理位置调用第三方API，工作台数据复用现有模块接口；
5.  **工作台特殊规则**：数据概览5分钟定时刷新+手动刷新，待办/通知实时更新；
6.  **日志统一**：工作台操作日志与系统设置模块日志同源，统一追溯。

---

## 五、非功能需求
### 5.1 性能要求
- 图片压缩后加载≤2s，批量导入/导出响应≤5s；
- 工作台数据刷新≤1s，待办操作响应≤0.5s；
### 5.2 安全性要求
- 操作日志永久保存，账号禁用后无法登录；
- 工作台快捷操作二次校验权限，避免越权；
### 5.3 兼容性要求
- 支持Chrome/Firefox/Edge浏览器，适配PC端（最小1366×768分辨率）；
### 5.4 可维护性要求
- 数据备份≤24小时，支持一键恢复，日志可导出；
### 5.5 易用性要求
- 工作台关键信息标红，操作流程与系统整体一致，降低学习成本。

---

## 六、验收标准
1.  **功能完整性**：所有模块功能无遗漏，工作台与其他模块联动正常；
2.  **数据一致性**：导入/导出数据无偏差，工作台数据与业务模块同步；
3.  **权限有效性**：权限控制严格，无越权操作，工作台内容按权限展示；
4.  **性能达标**：所有操作响应时间符合性能要求，无卡顿；
5.  **稳定性**：7×24小时运行，备份/恢复/通知功能正常；
6.  **交互合理性**：工作台操作流畅，跳转正确，符合管理员操作习惯。

---

## 七、使用说明
1.  本文档可直接复制保存为`后台管理系统功能需求逻辑文档.md`，适配VS Code/WebStorm等编辑器；
2.  开发人员：按菜单层级开发功能，重点关注工作台接口复用逻辑；
3.  产品人员：依据文档核对功能设计，确保覆盖所有需求点；
4.  测试人员：结合验收标准制定测试用例，验证功能完整性与数据一致性。