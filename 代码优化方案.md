# 代码优化方案 - 静态代码转动态代码

## 📊 当前问题分析

### 1. 代码现状
- **总数**: 26个Vue组件文件
- **问题**: 所有页面都是静态硬编码数据
- **影响**:
  - 数据无法更新
  - 无法实现分页、搜索等功能
  - 无法与后端API对接
  - 维护困难

### 2. 典型问题示例

#### party-work.vue (党建工作页面)
```vue
<!-- 静态硬编码数据 -->
<p>{{ "认真践行习近平总书记关于党的自我革命的重要思想" }}</p>
<p>{{ "2025-12" }}</p>
<p>{{ "06" }}</p>
<p>{{ "共计 10 条" }}</p>
<p>{{ "请输入您要搜索的内容" }}</p>
```

**问题**:
- ❌ 标题、日期、内容都是写死的
- ❌ 无法从API获取数据
- ❌ 无法实现点击跳转详情

### 3. 需要优化的文件分类

#### 列表页 (11个)
需要实现数据列表、分页、搜索：
- `party-building/party-work.vue` - 党建工作
- `party-building/team-work.vue` - 团建工作
- `party-building/members.vue` - 党员先锋
- `party-building/study.vue` - 党员学习
- `dynamic-news/index.vue` - 动态要闻列表
- `team-building/cases.vue` - 救援案例
- `team-building/showcase.vue` - 队伍风采
- `info-public/personnel.vue` - 人事任免
- `policy-regulations/laws.vue` - 法律法规
- `policy-regulations/regulations.vue` - 部门规章
- `policy-regulations/standards.vue` - 行业标准

#### 详情页 (8个)
需要根据路由参数获取数据：
- `party-building/party-work-detail.vue`
- `party-building/team-work-detail.vue`
- `party-building/member-detail.vue`
- `party-building/study-detail.vue`
- `dynamic-news/detail.vue`
- `team-building/showcase-detail.vue`
- `info-public/personnel-detail.vue`

#### 简单页面 (7个)
主要是静态展示内容：
- `home/Home.vue` - 首页
- `overview-info/leadership.vue` - 领导信息
- `overview-info/organization.vue` - 组织机构
- `overview-info/geography.vue` - 地理信息
- `team-building/about.vue` - 关于队伍
- `query-system/certificate.vue` - 证书查询
- `query-system/personnel.vue` - 人员查询
- `query-system/vehicle.vue` - 车辆查询

---

## 🎯 优化方案

### 方案一：完整前后端分离（推荐）

#### 1. 创建API接口层

**src/api/party.ts** - 党建相关接口
```typescript
import request from '@/utils/http'

// 获取党建工作列表
export function getPartyWorkList(params: {
  page: number
  pageSize: number
  keyword?: string
}) {
  return request.get('/api/party-work/list', { params })
}

// 获取党建工作详情
export function getPartyWorkDetail(id: string | number) {
  return request.get(`/api/party-work/${id}`)
}
```

**src/api/news.ts** - 新闻相关接口
```typescript
import request from '@/utils/http'

export function getNewsList(params: {
  page: number
  pageSize: number
  category?: string
}) {
  return request.get('/api/news/list', { params })
}

export function getNewsDetail(id: string | number) {
  return request.get(`/api/news/${id}`)
}
```

#### 2. 创建类型定义

**src/types/party.ts**
```typescript
export interface PartyWorkItem {
  id: number
  title: string
  summary: string
  content: string
  publishDate: string
  author: string
}
```

#### 3. 优化组件代码

**优化前 (party-work.vue)**:
```vue
<template>
  <p>{{ "2025-12" }}</p>
  <p>{{ "06" }}</p>
  <p>{{ "共计 10 条" }}</p>
</template>

<script lang="ts" setup></script>
```

**优化后**:
```vue
<template>
  <div class="party-work-list">
    <!-- 搜索栏 -->
    <el-input
      v-model="searchKeyword"
      placeholder="请输入搜索内容"
      @change="handleSearch"
    />

    <!-- 新闻列表 -->
    <div
      v-for="item in list"
      :key="item.id"
      class="news-item"
      @click="goToDetail(item.id)"
    >
      <p>{{ formatDate(item.publishDate) }}</p>
      <p>{{ item.title }}</p>
      <p>{{ item.summary }}</p>
    </div>

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="pagination.page"
      v-model:page-size="pagination.pageSize"
      :total="pagination.total"
      @current-change="fetchList"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { getPartyWorkList } from '@/api/party'
import type { PartyWorkItem } from '@/types/party'

const router = useRouter()

// 响应式数据
const list = ref<PartyWorkItem[]>([])
const searchKeyword = ref('')
const pagination = ref({
  page: 1,
  pageSize: 10,
  total: 0
})

// 获取列表数据
const fetchList = async () => {
  try {
    const res = await getPartyWorkList({
      page: pagination.value.page,
      pageSize: pagination.value.pageSize,
      keyword: searchKeyword.value
    })
    list.value = res.data.list
    pagination.value.total = res.data.total
  } catch (error) {
    console.error('获取数据失败:', error)
  }
}

// 搜索
const handleSearch = () => {
  pagination.value.page = 1
  fetchList()
}

// 跳转详情
const goToDetail = (id: number) => {
  router.push(`/party-building/party-work/${id}`)
}

// 格式化日期
const formatDate = (date: string) => {
  return new Date(date).toLocaleDateString('zh-CN')
}

// 初始化
onMounted(() => {
  fetchList()
})
</script>
```

---

### 方案二：使用Mock数据（快速开发）

如果后端API还未准备好，可以使用Mock数据：

**src/mock/party.ts**
```typescript
export const mockPartyWorkList = [
  {
    id: 1,
    title: '认真践行习近平总书记关于党的自我革命的重要思想',
    summary: '李炎溪，党的十八大以来，习近平总书记站在事关党的长期...',
    publishDate: '2025-12-06',
    author: '李炎溪'
  },
  {
    id: 2,
    title: '协同推进科学立法、严格执法、公正司法、全民守法',
    summary: '沈春耀，法治是治国理政的基本方式。党的二十届四中全会...',
    publishDate: '2025-12-06',
    author: '沈春耀'
  }
]
```

**组件中使用Mock数据**:
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { mockPartyWorkList } from '@/mock/party'

const list = ref(mockPartyWorkList)
</script>
```

---

## 📋 实施步骤

### 第一阶段：准备工作
1. ✅ 创建API接口层 (src/api/)
2. ✅ 创建类型定义 (src/types/)
3. ✅ 创建HTTP请求封装 (src/utils/http.ts)
4. ✅ 创建Mock数据 (src/mock/)

### 第二阶段：优化列表页 (优先级高)
1. `dynamic-news/index.vue` - 动态要闻列表
2. `party-building/party-work.vue` - 党建工作
3. `team-building/showcase.vue` - 队伍风采
4. 其他列表页...

### 第三阶段：优化详情页
1. 根据路由参数获取ID
2. 调用API获取详情数据
3. 渲染详情内容

### 第四阶段：优化查询系统
1. 实现表单验证
2. 实现查询接口对接
3. 展示查询结果

---

## 🎨 UI组件建议

推荐使用 Element Plus 组件：

### 列表页组件
- `el-card` - 卡片容器
- `el-input` - 搜索框
- `el-button` - 按钮
- `el-pagination` - 分页
- `el-empty` - 空状态

### 详情页组件
- `el-descriptions` - 描述列表
- `el-divider` - 分割线
- `el-back-top` - 回到顶部

---

## 📝 需要创建的文件

### API层 (8个文件)
```
src/api/
├── index.ts          # 统一导出
├── home.ts           # 首页接口
├── party.ts          # 党建接口
├── news.ts           # 新闻接口
├── team.ts           # 队伍建设接口
├── info.ts           # 信息公开接口
├── policy.ts         # 政策法规接口
└── query.ts          # 查询系统接口
```

### 类型定义 (6个文件)
```
src/types/
├── common.ts         # 通用类型
├── party.ts          # 党建类型
├── news.ts           # 新闻类型
├── team.ts           # 队伍类型
├── info.ts           # 信息公开类型
└── policy.ts         # 政策法规类型
```

### 工具函数 (3个文件)
```
src/utils/
├── http.ts           # HTTP请求封装
├── format.ts         # 格式化工具
└── validate.ts       # 验证工具
```

---

## ⚡ 快速开始示例

### 1. 创建HTTP封装

**src/utils/http.ts**
```typescript
import axios from 'axios'
import type { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'

const http: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_APP_BASE_API || '/api',
  timeout: 10000
})

// 请求拦截器
http.interceptors.request.use(
  (config) => {
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
http.interceptors.response.use(
  (response: AxiosResponse) => {
    return response.data
  },
  (error) => {
    console.error('HTTP Error:', error)
    return Promise.reject(error)
  }
)

export default http
```

### 2. 环境变量配置

**.env.development**
```bash
VITE_APP_BASE_API=/api
```

**.env.production**
```bash
VITE_APP_BASE_API=https://api.feibao-rescue.com/api
```

---

## 📊 优化效果对比

### 优化前
- ❌ 静态硬编码数据
- ❌ 无法更新内容
- ❌ 无法分页
- ❌ 无法搜索
- ❌ 维护困难

### 优化后
- ✅ 动态数据加载
- ✅ 支持实时更新
- ✅ 支持分页
- ✅ 支持搜索
- ✅ 易于维护
- ✅ 可对接后端API

---

## 🚀 下一步行动

1. **确认需求**: 与后端确认API接口规范
2. **选择方案**: 确定使用真实API还是Mock数据
3. **创建基础**: 先创建API层、类型定义、工具函数
4. **逐步优化**: 按优先级优化各个页面
5. **测试验证**: 确保功能正常

---

**文档版本**: v1.0
**创建日期**: 2025-01-28
**维护者**: 开发团队
