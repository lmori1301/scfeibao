# 头部导航调试指南

## 🔍 问题诊断

如果点击事件不工作，请按以下步骤排查：

### 步骤1: 检查浏览器控制台
1. 打开浏览器开发者工具（F12）
2. 查看Console标签页
3. 查看是否有红色错误信息

**常见错误**:
- `Failed to resolve component` - 组件导入问题
- `Unexpected token` - 语法错误
- `404 Not Found` - 文件路径错误

### 步骤2: 检查页面是否正常加载
访问: http://localhost:3000/

**应该看到**:
- ✅ 头部导航栏
- ✅ Logo（显示"飞豹"）
- ✅ 8个菜单项
- ✅ 页脚

**如果没有看到**:
- 检查开发服务器是否运行
- 刷新浏览器（Ctrl+F5 / Cmd+Shift+R）

### 步骤3: 手动测试路由

在浏览器地址栏输入以下路径测试：

```
http://localhost:3000/overview-info
http://localhost:3000/party-building
http://localhost:3000/dynamic-news
http://localhost:3000/team-building
```

**如果手动输入可以访问**:
- 路由配置正常
- 可能是Element Plus Menu组件问题

### 步骤4: 检查Element Plus Menu配置

当前Menu配置：
```vue
<el-menu
  :default-active="activeMenu"
  mode="horizontal"
  :ellipsis="false"
  router
  class="nav-menu"
>
```

**关键字段**:
- `router` - 启用路由模式（必需）
- `:default-active` - 激活菜单（必需）
- `mode="horizontal"` - 水平菜单（必需）

### 步骤5: 测试临时解决方案

创建一个简单的测试页面验证路由：

```vue
<template>
  <div>
    <h1>导航测试</h1>
    <router-link to="/overview-info">概况信息</router-link>
    <router-link to="/party-building">党建专栏</router-link>
    <router-link to="/dynamic-news">动态要闻</router-link>
  </div>
</template>
```

如果`router-link`可以工作，但Menu不行，说明是Element Plus Menu配置问题。

## 🛠️ 快速修复方案

### 方案1: 禁用Element Plus Menu的router模式

修改 `src/components/common/AppHeader.vue`:

```vue
<el-menu
  :default-active="activeMenu"
  mode="horizontal"
  :ellipsis="false"
  @select="handleMenuSelect"
  class="nav-menu"
>
```

添加处理函数：
```typescript
const handleMenuSelect = (index: string) => {
  router.push(index)
}
```

### 方案2: 使用router-link替代

如果Element Plus Menu有问题，可以用router-link:

```vue
<nav class="header-nav">
  <router-link to="/overview-info" class="nav-link">概况信息</router-link>
  <router-link to="/party-building" class="nav-link">党建专栏</router-link>
  ...
</nav>
```

## 📊 当前配置状态

### ✅ 已配置
- [x] Vue Router已安装
- [x] 路由模块已创建（8个）
- [x] 页面组件已创建（占位页面）
- [x] DefaultLayout已创建
- [x] Element Plus已安装
- [x] Pinia已安装

### ⚠️ 可能的问题
- [ ] Element Plus Menu router模式可能需要特殊配置
- [ ] CSS样式可能阻止点击事件
- [ ] 组件导入路径问题

## 🔧 调试命令

### 重启开发服务器
```bash
# 停止服务器: Ctrl+C
# 重新启动:
npm run dev
```

### 清除缓存
```bash
# 清除node_modules和重新安装
rm -rf node_modules
npm install
```

### 检查端口占用
```bash
# 检查3000端口是否被占用
lsof -i :3000
```

## 📞 反馈信息

请提供以下信息以便进一步诊断：

1. **浏览器控制台错误**（如有）
2. **点击菜单时的表现**
   - 完全没反应？
   - 有高亮但不跳转？
   - 其他表现？

3. **手动输入URL可以访问吗？**
   - 是 → 路由正常，Menu配置问题
   - 否 → 路由配置问题

4. **Network标签页状态**
   - 打开F12 → Network
   - 点击菜单
   - 是否有请求发出？

---

**文档版本**: v1.0
**创建时间**: 2025-01-28
