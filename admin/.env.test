# 测试/预发环境：使用 npm run dev:test 或 npm run build:test 时加载（Vite mode=test）
# 扫码验证：填测试环境对外可访问的「官网」根地址（手机能打开的 https/http），与测试站部署域名一致
VITE_APP_TITLE=四川飞豹救援后台管理系统（测试）
VITE_APP_BASE_API=/api
VITE_APP_PORT=5176
VITE_API_BASE_URL=http://localhost:3003
# 测试站官网根域名（人员扫码打开 /personnel/:id）；无 test 子域时可改为 https://www.scfb.org.cn 等
VITE_PORTAL_PUBLIC_URL=https://test.scfb.org.cn
# 高德 JSAPI Key：需将测试后台域名加入高德控制台 Web端 JSAPI 白名单。
VITE_AMAP_KEY=
VITE_AMAP_SECURITY_JSCODE=
# 本地真机联调可临时改为 http://本机IP:5173
# VITE_PORTAL_DEV_PORT=5173
